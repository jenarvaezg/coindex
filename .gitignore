.DS_Store
*.log
.playwright-mcp/

# Cachés de los scripts Python incluidos en skills del proyecto
__pycache__/
*.py[cod]

# Gradle y Android
.gradle/
/build/
/app/build/
/domain/build/
local.properties

# Firma: el keystore y sus contraseñas nunca entran en el repositorio
keystore.properties
*.jks
*.keystore

# Credenciales de la API de Numista, para los scripts. Misma regla que el keystore.
.env

# Capturas privadas de colección (FieldReportTest); nunca al remoto
.local/
